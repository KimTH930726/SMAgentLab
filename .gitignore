# 환경 변수 (비밀키 포함)
.env

# VOC 이메일 로컬 테스트 — refresh token이 그대로 저장되는 파일 (자격증명급 취급)
backend/scripts/.email_voc_token_cache.json

# 로컬 curl 테스트용으로 발급해둔 admin JWT — 자격증명급 취급
.voc_test_token.txt

# VOC 이메일 인수인계 문서 — 실제 client secret 값을 로컬 참조용으로 기록해두므로
# git에는 절대 올리지 않는다(§7 원칙). 팀 공유가 필요해지면 시크릿 값만 빼고
# 새 파일로 커밋할 것.
docs/tech/voc-email-handoff.md

# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
*.egg-info/

# Docker 볼륨 데이터
pgdata/

# 로컬 백업
*.sql
*.dump

# macOS
.DS_Store

# IDE
.idea/
.vscode/
*.swp

# Claude Code (로컬 설정 & 세션)
.claude/settings.local.json
.claude/todos/

# PyInstaller 빌드 중간 산출물 (dist/ 는 결과물이라 커밋함)
scripts/build/

# 실제 회사 정책서 원본 (파서 검증용, 절대 커밋 금지)
backend/tests/fixtures/real_policy_samples/
